/**
 * Represents an reward for watching a rewarded video.
 */
export type LevelPlayReward = {
  /**
   * The reward name.
   */
  name: string;
  
  /**
   * The reward amount.
   */
  amount: number;
};